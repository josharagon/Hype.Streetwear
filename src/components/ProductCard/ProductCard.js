import React, { useState } from "react";
import "./ProductCard.css";

const ProductCard = () => {
  return (
    <div>
      {/* <img
        src={require(`../../img/loading/${props.name}_0.jpg`).default}
        className="champion-card"
        alt={`${props.name} card`}
      ></img>
      <img
        src={require(`../../img/legacyLogos/${props.tags[0]}_icon.png`).default}
        className="legacy-logo"
        title={`Class: ${props.tags}`}
        alt={"champion class logo"}
      ></img>
      <div className="hover-message">
        <h2>{props.name}</h2>
        <h4>{props.title}</h4> */}
      {/* </div> */}
    </div>
  );
};

export default ProductCard;
